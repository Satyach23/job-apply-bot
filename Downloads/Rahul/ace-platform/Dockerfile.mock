FROM python:3.11-slim
WORKDIR /app
COPY mock_sources /app/mock_sources
RUN pip install --no-cache-dir fastapi uvicorn
ENV PYTHONPATH=/app
CMD ["uvicorn", "mock_sources.main:app", "--host", "0.0.0.0", "--port", "8200"]
