# ===========================================
# ACP Document Analyzer — Dependencies
# ===========================================

# --- Core Framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
jinja2>=3.1.0

# --- Data Validation ---
pydantic>=2.0.0

# --- Document Parsing ---
pypdf>=4.0.0          # PDF text extraction
python-docx>=1.0.0    # DOCX text extraction
striprtf>=0.0.26      # RTF text extraction
lxml>=5.0.0           # Enhanced XML handling

# --- LLM Client ---
openai>=1.0.0             # OpenAI-compatible SDK (for AI Gateway + Gemini)

# --- Embeddings & Vector Search ---
sentence-transformers>=2.7.0   # Local embedding model (all-MiniLM-L6-v2)
numpy>=1.24.0                  # Cosine similarity for SQLite backend

# --- Vector Store Backends (install one or all) ---
chromadb>=0.5.0                # ChromaDB backend (VECTOR_BACKEND=chroma)
faiss-cpu>=1.7.4               # FAISS backend (VECTOR_BACKEND=faiss)

# --- Environment ---
python-dotenv>=1.0.0

# --- UI (optional) ---
streamlit>=1.30.0
